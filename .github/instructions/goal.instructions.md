---
applyTo: '*'
---
# Product overview
This product is a command-line tool that automates Git operations using AI. 
- It generates commit messages from the diff from the previous commit. 
- Automatically add, commit, and push changes to the repository with only by specifying a branch name.